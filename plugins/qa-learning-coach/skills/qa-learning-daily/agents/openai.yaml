interface:
  display_name: "QA Learning Daily"
  short_description: "Plan one verifiable QA learning output"
  default_prompt: "Use $qa-learning-daily to verify the repository's current day before planning and recording today's automation testing output."

policy:
  allow_implicit_invocation: true
