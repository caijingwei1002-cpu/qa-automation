# Day 16

日期：2026-08-18
阶段：SauceDemo UI 框架
项目：test-projects/02-saucedemo-ui
主题：登录异常
知识点：负向场景与错误信息断言
当天任务：覆盖错误密码和空用户名

## 今日目标

- [x] 理解负向测试必须验证“正确失败”，而不只是没有成功
- [x] 区分身份认证（Authentication）与输入校验（Validation）
- [x] 新增错误密码测试
- [x] 新增空用户名测试
- [x] 验证错误提示正确且没有进入商品页
- [x] 保存验证结果到 `artifacts/day-016/`

## 今日学习与产出

负向 UI 测试需要同时验证两类结果：系统返回与失败原因匹配的错误反馈，并且没有进入成功页面。错误密码验证凭据认证失败；空用户名验证必填字段校验。由于业务规则不同，两个场景拆成独立测试函数。

目标文件：`test-projects/02-saucedemo-ui/tests/test_login.py`

新增产出：

- `test_wrong_password_shows_error`：有效用户名配合错误密码，断言认证错误文案和未进入 `/inventory.html`。
- `test_empty_username_shows_error`：空用户名配合有效密码，断言 `Username is required` 和未进入 `/inventory.html`。
- 保留原有标准用户登录 smoke 测试，验证 URL 和 `Products` 标题。

## 运行记录

目标测试：

```powershell
.\.venv\Scripts\python.exe -m pytest test-projects/02-saucedemo-ui/tests/test_login.py::test_wrong_password_shows_error -q
```

结果：`1 passed in 5.40s`

空用户名测试：

```powershell
.\.venv\Scripts\python.exe -m pytest test-projects/02-saucedemo-ui/tests/test_login.py::test_empty_username_shows_error -q
```

结果：`1 passed in 6.12s`

完整登录测试文件：

```powershell
.\.venv\Scripts\python.exe -m pytest test-projects/02-saucedemo-ui/tests/test_login.py -q
```

首次结果：`3 passed in 10.84s`
补充文件末尾换行后重新验证：`3 passed in 12.67s`

静态检查：`git diff --check` 无错误；仅出现 Windows LF/CRLF 转换提示。

证据：`artifacts/day-016/verification.md`

## 问题与解决方案

本日没有业务断言失败。测试使用项目虚拟环境并在允许启动 Playwright 子进程的环境中执行；此前记录的 `WinError 5` 是浏览器启动权限层阻塞，不是登录测试逻辑失败。

检查发现目标文件末尾缺少换行符，补齐后重新运行完整测试，结果仍为 `3 passed`。变更范围只有目标测试文件和本日证据，不涉及 fixture、pytest 配置或其他项目。

## 知识验收

我能够说明：错误提示证明系统是否正确反馈失败原因；不进入商品页证明错误操作没有进入成功状态。错误密码属于 Authentication，空用户名属于 Validation；测试失败时先排查环境，再检查 locator、数据和断言，最后才判断产品行为是否违反预期。

## 知识落盘记录

知识库文件：`LEARNING-NOTES.md`
章节：Day 16：登录异常
本次新增的理论内容：负向测试的双重证据、Authentication 与 Validation 的业务规则区别、错误信息断言、成功状态排除、测试拆分和假通过风险。
自包含复习检查：是；只阅读 Day 16 章节即可理解负向登录测试的设计、断言含义和失败排查顺序。
目录和主题索引是否已同步：已同步。

## 复盘

今天的知识已落实到两个独立负向测试中，每条测试都保留业务意图注释，并同时包含错误文案和成功路由排除。只断言“不跳转”会产生假通过，按业务规则拆分测试则能让失败定位更清晰。

## 明天第一步

Day 17：在 `test-projects/02-saucedemo-ui/tests/test_users.py` 中覆盖 `locked_out_user` 与 `problem_user`，学习测试账号如何代表不同风险场景。
